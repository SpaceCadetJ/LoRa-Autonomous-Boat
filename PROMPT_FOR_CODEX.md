# Prompt for the Codex agent (paste as-is)

Continue the LoRa Autonomous Boat project as the second agent, working in parallel with the primary agent (Claude Code) on branch `v1-kicad-reconstruction`. Read, in order: `HANDOFF.md` (last section "Primary ... Phase A released"), `FILE_OWNERSHIP.md` (last table), `BLOCKERS.md` (last section), `reviews/codex/README.md`. The primary released Phase A at commit `639e08f`: `hardware/kicad/` opens in KiCad 9.0.7, DRC 0 errors, ERC 0 violations, `hardware/kicad/tools/check_netlist.py` PASS, Gerber XOR vs the fab films F.Cu 0.87 % / B.Cu 0.23 %. Your findings R01-R03, R06-R08 are dispositioned in that HANDOFF section; do not re-open them without new evidence.

Ownership from now on. You have exclusive write access to `pm/**`, `docs/atlas/**`, `reviews/**`, and you may append dated sections to the four root coordination files. Do not write anywhere else (`hardware/**`, `firmware/**`, the rest of `docs/**`, `README.md`, `REPORT.md`, `CONVERSION_LOG.md` are the primary's). Do not run the primary's generators or `build_v1.py`; read their outputs in `hardware/kicad/_build/` if present, or regenerate only into a scratch copy outside the repo. Never edit `Allegro/**`. Git: commit only files under your own paths, on this branch, never rebase or amend; the primary commits everything else.

Work these packets in order, one commit each, and record each in `HANDOFF.md`:

1. S2 independent verification of the released revision. Using only `kicad-cli` and your own scripts under `reviews/codex/tools/`, re-derive from `hardware/kicad/LoRa_Boat_Controller.kicad_sch` + `.kicad_pcb`: the schematic netlist vs `pstxnet.dat` pin sets (refs carry a "1" suffix where the Allegro refdes lacked a digit; use the `Allegro_RefDes` property), DRC/ERC counts, and a drill/hole comparison against the v5 pad flashes. Write `reviews/codex/S2_VERIFICATION.md` with exact commands, hashes, and a verdict per gate. Report disagreements with the primary's numbers as findings, not edits.

2. Interactive project manager in `pm/`: a single self-contained `pm/index.html` (no build step, no external CDN except cdnjs/jsdelivr) that reads `pm/status.json` and shows: phases A0-A4, B, C with gate status and evidence links; the decision list from `BLOCKERS.md` with the primary's defaults; open findings R01-R11 with disposition; a file map of the repo (clickable relative links to docs, images, KiCad files); and a "what to review next" list for Jay. You own `pm/status.json`; populate it from `HANDOFF.md`, `AGENT_STATUS.md`, `BLOCKERS.md`, `CONVERSION_LOG.md`, `docs/A0_PROVENANCE.md` and the `_build/*.json` reports (copy the numbers you cite into status.json with the source path). Keep it readable on a phone. Add a one-paragraph `pm/README.md` on how to open it (double-click, or `python -m http.server` from repo root).

3. S3 connector atlas in `docs/atlas/`: one Markdown page per external connector (LORAMODULE, GPSMODULE, SPEEDCONTROLLER, STEERINGSERVO, CANHEADER, JTAG, VIN/GND pads, TP1-TP5) with a pin table: pin number, KiCad net, Allegro net, MCU pad, firmware function (cite `firmware/Core/Src/main.c` lines), voltage/direction, expected bench reading; plus `docs/atlas/README.md` index. Source of truth is `pstxnet.dat` and the released schematic; note V1 defects inline (PB0 driven low onto GPS pin 3, CAN RXD on PA10 which has no CAN function, both actuator header pin 1s on +3V3, CEXT returned to +3V3 instead of VSS, VBAT and BOOT0 floating).

4. S4 radio/voice requirements draft in `reviews/codex/RF_VOICE_REQUIREMENTS.md`: for Jay's handheld with voice + text + location, compare live push-to-talk vs recorded voice over LoRa/FSK with concrete numbers (codec2 1.2/2.4/3.2 kbps framing, SX126x FSK or LoRa SF/BW airtime per frame, 915 MHz ISM duty and power limits in the US, link margin with the RYLR998 22 dBm / -129 dBm figures already in `docs/research/DATASHEET_NOTES.md`), and a coexistence policy with the armed-RC command deadline. End with numbered REQ-RF-xx / REQ-VOC-xx candidates the primary can merge into `docs/V2_REQUIREMENTS.md`.

Timebox each packet; if blocked, write what you have and the blocker into `HANDOFF.md`. Finish every session with: files changed, commands run, findings, next smallest task.
