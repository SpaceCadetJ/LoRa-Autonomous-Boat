# Prompt for the Codex agent (paste as-is)

Continue the LoRa Autonomous Boat project as the second agent, working in parallel with the primary agent (Claude Code). The working branch is now `v2-design` (created from `v1-kicad-reconstruction`; the V1 baseline is tag `v1-kicad-baseline`). Read, in order: `REPORT.md` (whole file, 5 minutes), `HANDOFF.md` (last two sections), `FILE_OWNERSHIP.md` (last table), `BLOCKERS.md` (last section), `docs/V2_REQUIREMENTS.md`, `hardware/kicad_v2/README.md`. Your earlier files are committed as-is (`pm/tools/*.mjs`, `reviews/codex/s2/**`, `reviews/codex/firmware_prototype/**`, commit `0aa4920`); nothing of yours was edited.

State of the primary's work you can now build on:

- V1 (Phase A) is closed at tag `v1-kicad-baseline`: `hardware/kicad/` opens in KiCad 9.0.7, DRC 0 errors, ERC 0, netlist == `pstxnet.dat`, Gerber XOR vs films F.Cu 0.87 % / B.Cu 0.23 %, drill 53/53. Findings R01-R03 and R06-R08 are dispositioned in HANDOFF; do not re-open without new evidence.
- Phase B is complete: `docs/V1_DESIGN_REVIEW.md` (35 findings F-xx with copper/firmware evidence), `docs/V2_REQUIREMENTS.md` (45 REQ-xx + decision table), `firmware/V2_FIRMWARE_PLAN.md`.
- Phase C draft is in `hardware/kicad_v2/` (tag `v2-design-draft` when you read this, else HEAD of `v2-design`): 6-sheet schematic (ERC 0 errors, 1 accepted warning), 80 x 46 mm board with 118 parts / 71 nets, autorouted with freerouting, DRC 0 violations, `docs/BOM_V2.csv` (68 lines, $53.80 board-only at qty 1), renders `docs/img/v2_pcb_top.png` / `v2_pcb_bottom.png`, schematic `docs/img/v2_sch_*.svg` and `v2_schematic.pdf`. Everything there is generated from `hardware/kicad_v2/tools/v2_design.py` (parts, nets, placement, prices) by `gen_v2.py`; routing by `route_v2.py`. Treat those files as read-only inputs.

Ownership is unchanged: you have exclusive write access to `pm/**`, `docs/atlas/**`, `reviews/**`, and you may append dated sections to `HANDOFF.md`, `AGENT_STATUS.md`, `BLOCKERS.md`, `FILE_OWNERSHIP.md`. Do not write anywhere else and never edit `Allegro/**`. Do not run the primary's generators inside the repo (they overwrite `hardware/kicad_v2/*.kicad_*`); if you need to re-run something, do it on a copy outside the repo. Git: commit only files under your own paths, on `v2-design`, never rebase, amend or force-push. Report disagreements with the primary's numbers as findings, not edits.

Work these packets in order, one commit each, and record each in `HANDOFF.md`:

1. Finish the interactive project manager in `pm/`: a single self-contained `pm/index.html` (no build step required to view it; your `tools/build.mjs` may generate `pm/status.json`, but the page must also open from a double-click) that shows: phases A0-A4 / B / C with gate status and evidence links; the decision list from `BLOCKERS.md` with the defaults in use (note decision 7 is now 80 x 46 mm, 72 x 38 mm hole pattern); your findings R01-R11 with disposition; the V1 finding list F-xx from `docs/V1_DESIGN_REVIEW.md` linked to the REQ-xx that closes each one; a V2 page with the board render, sheet SVGs, BOM_V2 table and the "before fabrication" list from `REPORT.md` §4; a repo file map with clickable relative links; and a "what Jay should review next" list. Keep it readable on a phone. `pm/README.md`: how to open it.

2. S2 verification write-up: you already have `reviews/codex/s2/` (verify.py, drc.json, erc.json, netlist.xml, input manifest). Turn it into `reviews/codex/S2_VERIFICATION.md` with exact commands, hashes and a verdict per gate against the V1 numbers above, then run the same scripts against `hardware/kicad_v2/` and add a V2 section (ERC/DRC counts, unconnected count, part and net totals, BOM line count) so the primary's claims are independently confirmed or disputed.

3. Independent V2 design review, `reviews/codex/V2_DESIGN_REVIEW.md`: read the six V2 sheets and `v2_design.py` against `docs/V2_REQUIREMENTS.md` and the datasheets in `docs/research/DATASHEET_NOTES.md`. For each REQ-xx say met / not met / cannot tell, with the sheet and refs. Specifically check: R1240N feedback and compensation values (R2/R3/C3, R4/R5/C4), TPS54202 feedback divider (R8/R9) and bootstrap, INA180 gain vs shunt full scale, 74AHCT1G125 supply and enable wiring, ICM-20948 pin usage (AD0/SDO tie, REGOUT cap, VDDIO), crystal load capacitors vs ABM8/ABS07 CL, USB-C CC pull-downs, PESD placement, XT30 current path width. Number findings V2-R01..; severity and the exact change you propose. The primary will fold accepted items into `v2_design.py`.

4. Connector atlas in `docs/atlas/`: one page per external connector for V1 (LORAMODULE, GPSMODULE, SPEEDCONTROLLER, STEERINGSERVO, CANHEADER, JTAG, VIN/GND, TP1-TP5) and V2 (J1 XT30, J2 ESC, J3 SERVO, J4 LoRa, J5 GNSS, J6 USB-C, J7 SWD, TP1-TP7) with pin number, net, MCU pad, firmware function (cite `firmware/Core/Src/main.c` lines for V1, `firmware/V2_FIRMWARE_PLAN.md` for V2), voltage/direction and expected bench reading; `docs/atlas/README.md` as index. Note the V1 defects inline (PB0 driven low onto GPS pin 3, CAN RXD on PA10, both actuator header pin 1s on +3V3, CEXT to +3V3, VBAT/BOOT0 floating).

5. RF / voice requirements draft, `reviews/codex/RF_VOICE_REQUIREMENTS.md`: for Jay's handheld with voice + text + location, compare live push-to-talk vs recorded voice over LoRa/FSK with numbers (codec2 1.2/2.4/3.2 kbps framing, SX126x FSK or LoRa SF/BW airtime per frame, US 915 MHz ISM duty and power limits, link margin with the RYLR998 22 dBm / -129 dBm figures in `docs/research/DATASHEET_NOTES.md`), and a coexistence policy with the armed-RC command deadline (REQ-CTL-01 failsafe, 500 ms). End with numbered REQ-RF-xx / REQ-VOC-xx candidates the primary can merge into `docs/V2_REQUIREMENTS.md`.

Timebox each packet; if blocked, write what you have and the blocker into `HANDOFF.md`. Finish every session with: files changed, commands run, findings, next smallest task.
